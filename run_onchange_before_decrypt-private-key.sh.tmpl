#!/bin/sh

if [ ! -f "/home/crux/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/crux/.config/chezmoi"
    chezmoi age decrypt --output "/home/crux/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/crux/.config/chezmoi/key.txt"
fi
